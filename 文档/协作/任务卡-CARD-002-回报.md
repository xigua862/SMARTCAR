# 任务卡-CARD-002-回报
- 日期：2026-09-25　执行人：工人
- 结论：**完成**。编码器体检 + 3 次仪表跑数据全齐；无代码改动、无 GOURD_* 开关改动（只改了 2 份文档的烧录章节 + 新增 `_tmp` 录制脚本）。两处执行偏差：① 出口 O 打点因"人喊→AI 发令"链路延迟追不上车速，按用户指令改为 `TEST 2` 直接吃 100Hz 回放（出口 OD 从回放里读）；② 3 次发车位不完全一致（run1=练习发车点，run2/3=最后一个圆内），见下。

---

## 步骤 0 · 左右编码器体检（手转 10 圈，单位 mm）

| 项 | 原始值 |
|---|---|
| 左轮 10 圈 | `L=+2339  R=0` |
| 右轮 10 圈 | `L=-8  R=-2468` |
| 理论值（10 圈 × 207.3mm） | ≈2073 |
| 静止 10 秒 ×2 次 `O` | `OD=1246 L=-8 R=-2468` / `OD=1246 L=-8 R=-2468`（**两次完全相同，无虚计** ✅） |

- 两轮对比：2339 vs 2468，差 **5.5%** —— 超卡的 ≤1% 判据线，按卡要求报上来（注：手转圈数精度有限，两次都可能不是整 10 圈；但"都比理论值大 13~19%"这个现象也原样给出）
- 静止虚计检查 ✅ 通过

## 步骤 1 · 准备核对
- `V` 横幅确认：`tag : 0925-1738 built: Sep 25 2026 17:39:37` ✅ 就是新基线
- GOURD 触发 grep 核对（未改）：`GOURD_EXIT_USE_PATTERN 0`、`GOURD_EXIT_ON_LOST 0`、`USE_GOURD_EXIT_ODOM 0`、`GOURD_EXIT_USE_GW 0` ✅ 全关
- 录制工具：新写 `_tmp/card002_run.ps1`（R 起跑长录，带 ms 时间戳）和 `_tmp/cap_test.ps1`（发令+定时录制），输出到 `_tmp/card002_runN.txt`

## 步骤 2 · 仪表跑 ×3（原始文件，未加工）

| 次 | 方式 | 发车位 | 文件 | 关键尾行 |
|---|---|---|---|---|
| run1 | `R` 起跑长录 ~120 秒（含绕圈全程 + 急停自动回放） | 练习发车点（葫芦入口附近） | `_tmp/card002_run1.txt`（3707 行） | `ST=4 OD=19334 GW=1 IG=1` |
| run2 | `TEST 2`（循迹 2 秒+100Hz 回放） | 最后一个圆内 | `_tmp/card002_run2.txt`（226 行） | `OD=20243 GW=1 IG=1`；过程中 `GZ=+282~297`（**没削顶**，量程修复实锤）、`GOURD-ENTRY mark set/cleared`、`BRANCH #61` |
| run3 | `TEST 2` | 最后一个圆内（同 run2 手法） | `_tmp/card002_run3.txt`（224 行） | `OD=21240 GW=2 IG=1`；`BRANCH #64~66` |

- run1 用户目击：进葫芦后第 1、2 个圆各转两圈，之后在第 3 个圆内一直转（触发全关，出不去=预期）。
- run2 用户目击：绕最后一个圆一圈，**过了出口、过了最后圆与倒数第二圆的切点**。
- ⚠️ OD_exit 没有独立打点值：原卡"人盯出口→发 O"链路实测追不上车（人发消息→AI 发串口的延迟 ≫ 车过口时间）。run2/run3 的 100Hz 回放里含过出口全过程的 OD，请军师直接从轨迹读出口点 OD；run1 的全程时间线里每次过口也有 OD（50ms 一拍短行）。
- 3 次发车位：不完全一致（方案中途按用户指令从"R 起跑"改为"最后圆内 TEST 2"），偏差说明即本行。

## 步骤 3 · 文档更新（已做）
- `文档/交接-双AI分工-20260925.md` 第七节：烧录命令改 CMSIS-DAP；注明"一条 DAPLink USB 同干烧录+串口、PC→车 RX 已通、旧 ST-Link+CH340 双线流程作废、烧录和读串口仍别同时做"。
- `文档/给队友的交接-20260924晚.md` 第一节：同步更新 + 指明"以分工文档第七节为准"。

## 验收逐条对照
- ✅ 步骤 0：左/右 10 圈计数两个数（2339/2468）+ 差 5.5%；静止 10 秒前后计数两行（纹丝不动）
- ✅ 3 次仪表跑：每次一个录制文件（路径见表），文件里有短行时间线（`GW=/IG=/OD=/GZ=`）；run1 含急停回放段。⚠️ OD_exit 无独立打点（原因见上），回放可读
- ⚠️ 3 次发车位：不完全一致（已注明原因）
- ✅ 原始数据未加工
- ✅ 无代码改动、无 GOURD_* 开关改动（只动 2 份文档 + `_tmp` 脚本）

## 卡点
1. 手转 10 圈两轮差 5.5%（超 1% 线）——但手转精度有限，是否采信/要不要架空电机驱动重测，军师定。
2. 出口 O 打点方案不可行（延迟），已改 `TEST 2` 回放方案——下张卡若还要 OD_exit，建议直接从 run2/run3 回放读，或让车在出口前 `TEST` 起跑。
3. run1 结尾 `GW=1`、run3 结尾 `GW=2`（IG 都=1）——GW 在三圈内有累计动作，干净与否军师看文件。
