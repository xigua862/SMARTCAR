# 任务卡-CARD-003-回报（IG清零重构与翻转修正重启）
- 日期：2026-09-25　执行人：工人
- 结论：**代码部分完成**（0E0W、已烧录 FW:0925-2101、commit `c39c460` 已 push）；**验证跑完成**：C1 回归符合预期条款，C2 葫芦跑取得决定性数据 —— 车**按顺序过圆直达最后一个圆**（历史首次），但未出圈，用户锁车。按 C3 规则【未改任何参数】，原始数据如下。

---

## Part A/B 改动清单（与卡逐项核对）

| 卡要求 | 执行 | 状态 |
|---|---|---|
| A1 新增 `GOURD_EXIT_STRAIGHT_FRAMES 100` / `GOURD_IG_FAILSAFE_FRAMES 3000` | app_config.h 葫芦块，原值未动 | ✅ |
| A2 四个静态变量（ge_confirmed/ge_done/ge_straight/ge_stay），与 ge_in_gourd 同 #if 同进同出 | line_follow.c ≈203-208 行 | ✅ |
| 置位点：gourd_waves++ 同处置 ge_confirmed；GW 归零处置 ge_done | ≈424、430 行 | ✅ |
| s_ig_leave 清 GW 同处清 confirmed/done/straight | ≈949-951 行 | ✅ |
| line_follow_reset→实际为 line_follow_init（代码里无 reset 函数），四变量与 ge_in_gourd 同处清 | ≈1762-1765 行 | ✅ |
| A3 三路径：Path②（!ge_confirmed 旧快清原样）+ Path①（done+100拍普通线）+ Path③（3000拍兜底）；0→1 边沿 ge_stay=0 | ≈800-920 行 | ✅ |
| A4 s_ig_leave 段一字不动 | 只加了卡要求的同行三清 | ✅ |
| B：GOURD_USE_FLIP 0→1、GOURD_FLIP_ON_NORMAL 0→1，其余不动 | app_config.h ≈955、964 行 | ✅ |
| 别动清单（EXIT_ODOM/USE_GW/USE_PATTERN/ON_LOST 全 0、SLOWDOWN 0、ARC_FLIP 0、NO_CROSS 1） | 未碰 | ✅ |
| FW_TAG 真实时间 | `0925-2101` | ✅ |
| 编译 0 Error 0 Warning | Code=26032（+344B） | ✅ |

偏差说明：卡写"`line_follow_reset()` 四变量全清"，代码中无此函数，复位点在 `line_follow_init()`（ge_in_gourd 也在此处复位），已按"与 ge_in_gourd 同处"执行。

## C1 回归跑（圈外，TEST 2 ×2）

| 趟 | 路段 | 结果 | 文件 |
|---|---|---|---|
| 1 | 直道+S弯 | mark set/cleared ×2 对（Path② 快清正常），无 GOURD-TANGENT，结束 GW=0 IG=0 | `_tmp/card003_c1_run1.txt` |
| 2 | 弯道段 | mark set ×3 / cleared ×2；第 3 次未清——车停在线外（IR=00000000 非普通线，20拍计数不走、TTL600 未到），末尾 IG=1 挂着 | `_tmp/card003_c1_run2.txt` |

- 用户目击（两趟）：**循迹肉眼无变化** ✅
- 弯道误点 IG 仍存在，但回到普通线即被 Path② 清掉（符合卡 C1 预期条款）；GZ 峰值 -415 无削顶。

## C2 葫芦穿越跑（练习发车点发车，长录 81.3 秒）

文件：`_tmp/card002_run4.txt`（3820 行全量，含急停自动回放前 158 拍，回放因我停录制被截断，短行 20Hz 全程完整）。

**用户目击：从入口进去后按顺序过圆、一直走到最后一个圆，但最后没出去；捡车锁车。（"这一次很可以"）**

### 事件时间线（毫秒为录制时间戳，R 在 500ms 发出）

| 时间 | 事件 |
|---|---|
| 3.8~6.4s | mark set/cleared ×5 对（入口前误点，Path② 快清全部正常释放） |
| 6.7s | 第 1 个 TANGENT（OD=1731 GW=0） |
| 6.7~11.9s | TANGENT ×3（OD 1731/2425/3309，间距 694/884mm），GW 0→1→2 归零 → **Path① "cleared (done+straight)" 首次触发** |
| 11.9~33.2s | 同样模式再循环 4 轮：TANGENT 每轮 3~4 个 + done+straight 清（共触发 6 次） |
| **33.3s** | TANGENT（OD=13634 GW=2）后**丢线 IR=00000000，RPM→0，原地静止** |
| 33.5~63.2s | **静止约 30 秒（OD 停 13756，GW=2 IG=1 全程不动）—— 用户确认：人为捡车放回线上** |
| 63.2~65.2s | mark set/cleared ×5（重新上线），TANGENT 恢复 |
| 65.2~81.2s | 又过 TANGENT ×5（OD 15322/16315/16535/17275/18394） |
| 81.3s | 用户长按急停：`STOP by KEY(long press)`，OD=19707mm，**终态 GW=2 IG=1** |

### 关键数据（原样）

1. **TANGENT 共 23 行**，其中 5 对间距仅 204~232mm：(6001,6233) (7087,7319) (9109,9336) (12793,12997) (16315,16535) —— 正是卡 C3 预判的"每切点 2 次"形态（其余间距 621~993mm）。
2. **Path① "done+straight" 触发 6 次**，全部在葫芦区域内（GW 因双打提前归零 → ge_done 提前置位 → 圈内出现连续 100 拍普通线判定）。每次清完 100~600ms 内 mark 重新 set（车还在葫芦图案上）。
3. **IG 稳定性（Part A 主目标）**：绕圈期间 IG 再未出现旧病"圈内乱跳 0"；30 秒静止期间 GW=2 IG=1 纹丝不动（`ge_stay` 在跑但未及 3000 拍兜底）。
4. **翻转修正**：代码无打印（静默覆盖 e 15 拍，与 TANGENT 同一道门，TANGENT 触发即武装）；用户目击"按顺序过圆到最后一个圆"即为间接证据 —— 对照 CARD-002 run1"每个圆绕两圈才过去"，改善明显。
5. **未出圈原因（数据层面）**：四个出口触发开关按卡保持全 0，固件没有任何"出圈转向"动作；GW 双打导致归零时机与物理出口错位，Path① 清 IG 也未产生任何转向。

## 给军师的判读素材（不加工）

- C3 分支命中：**"一趟跑出现每切点 2 次 TANGENT"** —— 23 行里 5 对 ~220mm 双打。若按下张卡 GOURD_TOTAL=6，清零判据按归零、形式不变（卡原文已授权此路径）。
- 新现象：Path① 的"连续 100 拍普通线"在葫芦区域内会成立（双打提前归零后），IG 清了又立刻重 set —— 本次无害，但如果未来出口动作挂 IG/ ge_done，会被这个提前清零影响。
- 33 秒丢线静止段是人为因素，数据分析时建议剔除 33.3~63.2s 区间。
- C1 遗留：弯道误点 IG 仍在（Path② 200ms 内释放，符合预期）；TEST 2 结束若车停在线外，IG 会挂着等 TTL —— 已知边界。

## 验收对照
- [x] 编译 0E0W、FW_TAG 真实时间、commit 只 add 具体文件、已 push
- [x] C1 两趟文件 + 目击
- [x] C2 长录文件 + 目击 + 终态
- [x] 未改任何卡外参数；别动清单未碰
