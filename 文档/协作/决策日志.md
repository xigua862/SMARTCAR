# 决策日志（军师维护，一行一条）

> 作用：防止两个对话口径漂移。军师每拍板一件事就追加一行；工人有疑义在回报里写，不改这里。
> 军师对话上下文爆了新开时，先读这份再读 `文档/交接-双AI分工-20260925.md`。

| 日期 | 决策 | 为什么 | 否掉/替代了什么 |
|---|---|---|---|
| 2026-09-24 | 相切点判据三条定稿：宽(≥5路)+只贴一端 且 GZ±8拍内翻号 且 事件后6拍内无全灭；空隙>1路即算两组 | 实测数据（`_tmp/test2.txt`，工具 `t2.ps1`） | 单靠宽图案（直角弯也命中，不够） |
| 2026-09-24 | PC→车串口 RX 不通 → 改用"长按急停自动回放 6 秒轨迹"取数据 | 串口发不进去，按键方案实测可行 | 串口在线下发轨迹 |
| 2026-09-24 | 修掉 `GYRO_CONFIG` 量纲 bug（GZ 原先放大 8 倍） | 读 1520~1990 实际应 ±250 | 旧量纲下的所有 GZ 判据阈值作废 |
| 2026-09-25 | **工作方式改为"军师(只决策) + 工人(只执行)"双对话，接口 = `文档/协作/` 任务卡** | 决策与执行分开：治"拍脑袋"和"边想边改编译挂"两个老毛病 | 单对话边想边做 |
| 2026-09-25 | 参考资料评估：5 份里只有马铃薯国赛光电管的"地图回放导航"思路可借鉴，列入二期（开分支实验）；摄像头/纯跟踪/壁爬资料与本机硬件（8 路数字红外、无摄像头）不匹配 | 当前路线接近跑通、规则按距离排名稳定性优先；回放方案命脉是陀螺，须先修 IMU，现在切换=放弃已验证成果重写控制层 | 立即切换地图回放分支 |
| 2026-09-25 | 采纳队友 0921 交接固件为新基线（CARD-001 Part A）；原"修 IMU present"前置项作废 | 队友 `imu.c` 已含 WHO 重试 3 次+原始读数兜底，并修了我们不知道的新 bug：陀螺 ±250dps 量程写入不生效→GZ 削顶（-247/+250 签名）→历史"转过头/转不够"根源之一 | 自己按原计划重写 IMU 初始化 |
| 2026-09-25 | 直行平衡点坐在 L2/L3（ERR≈-8）判为恒定偏置，立 T1 电气中心 / T2 机械对称 / T3 闭环存档三组判别实验（CARD-001 Part B） | 代码侧已排除（两版权重表均对称 -7…+7）；偏置被 PD 吃掉=一侧修正余量永久占用，高速必有一侧先冲出，必须找源消掉 | 放任 PD 继续抵消偏置 |
| 2026-09-25 | `WHO=70` 判为正常，验收口径从 68 改 70 | 芯片是 MPU6500（0x70）不是 MPU6050；旧代码 `present=(who==0x68)`（git 0158c36 实锤）→ 历史 IMU=0/零偏跳过/IG 乱跳的病根是认错芯片型号，不是读挂 | "MPU6050 读挂"的旧叙事 |
| 2026-09-25 | 直线偏置结案：T1 电气中心准（H2 排除）；T3 平衡点已回中心、L2/L3 未复现，判为左编码器接触不良期速度环吃坏反馈；闭环残余 3.8% 不阻塞 | CARD-001 回报数据；左编码器体检列入 CARD-002 步骤 0 | 继续追查传感器/重调电位器 |
| 2026-09-25 | 葫芦路线：查明队友版已实现 GW 门控+强转且实测失败过（0924 日志：IG 乱跳抹 GW + GZ 削顶），两根因恰被新基线修掉但未实车验证 → CARD-002 触发全关仪表跑拿干净数据，再定开判据④（GW）还是③（里程，24847 已作废待重量） | 失败数据在 zip 文档里（实车日志证据-葫芦圈-20260924.md），不浪费实车试错 | 盲目重开 GW 触发；沿用 24847mm 里程阈值 |
| 2026-09-25 | 记账待查（不阻塞葫芦）：0924 日志证据 A"全灭仍直行、丢线兜底没生效" | 车直着开出赛道=白丢一轮，但先保葫芦主线 | —— |
