# 决策日志（军师维护，一行一条）

> 作用：防止两个对话口径漂移。军师每拍板一件事就追加一行；工人有疑义在回报里写，不改这里。
> 军师对话上下文爆了新开时，先读这份再读 `文档/交接-双AI分工-20260925.md`。

| 日期 | 决策 | 为什么 | 否掉/替代了什么 |
|---|---|---|---|
| 2026-09-24 | 相切点判据三条定稿：宽(≥5路)+只贴一端 且 GZ±8拍内翻号 且 事件后6拍内无全灭；空隙>1路即算两组 | 实测数据（`_tmp/test2.txt`，工具 `t2.ps1`） | 单靠宽图案（直角弯也命中，不够） |
| 2026-09-24 | PC→车串口 RX 不通 → 改用"长按急停自动回放 6 秒轨迹"取数据 | 串口发不进去，按键方案实测可行 | 串口在线下发轨迹 |
| 2026-09-24 | 修掉 `GYRO_CONFIG` 量纲 bug（GZ 原先放大 8 倍） | 读 1520~1990 实际应 ±250 | 旧量纲下的所有 GZ 判据阈值作废 |
| 2026-09-25 | **工作方式改为"军师(只决策) + 工人(只执行)"双对话，接口 = `文档/协作/` 任务卡** | 决策与执行分开：治"拍脑袋"和"边想边改编译挂"两个老毛病 | 单对话边想边做 |
| 2026-09-25 | 参考资料评估：5 份里只有马铃薯国赛光电管的"地图回放导航"思路可借鉴，列入二期（开分支实验）；摄像头/纯跟踪/壁爬资料与本机硬件（8 路数字红外、无摄像头）不匹配 | 当前路线接近跑通、规则按距离排名稳定性优先；回放方案命脉是陀螺，须先修 IMU，现在切换=放弃已验证成果重写控制层 | 立即切换地图回放分支 |
| 2026-09-25 | 采纳队友 0921 交接固件为新基线（CARD-001 Part A）；原"修 IMU present"前置项作废 | 队友 `imu.c` 已含 WHO 重试 3 次+原始读数兜底，并修了我们不知道的新 bug：陀螺 ±250dps 量程写入不生效→GZ 削顶（-247/+250 签名）→历史"转过头/转不够"根源之一 | 自己按原计划重写 IMU 初始化 |
| 2026-09-25 | 直行平衡点坐在 L2/L3（ERR≈-8）判为恒定偏置，立 T1 电气中心 / T2 机械对称 / T3 闭环存档三组判别实验（CARD-001 Part B） | 代码侧已排除（两版权重表均对称 -7…+7）；偏置被 PD 吃掉=一侧修正余量永久占用，高速必有一侧先冲出，必须找源消掉 | 放任 PD 继续抵消偏置 |
| 2026-09-25 | `WHO=70` 判为正常，验收口径从 68 改 70 | 芯片是 MPU6500（0x70）不是 MPU6050；旧代码 `present=(who==0x68)`（git 0158c36 实锤）→ 历史 IMU=0/零偏跳过/IG 乱跳的病根是认错芯片型号，不是读挂 | "MPU6050 读挂"的旧叙事 |
| 2026-09-25 | 直线偏置结案：T1 电气中心准（H2 排除）；T3 平衡点已回中心、L2/L3 未复现，判为左编码器接触不良期速度环吃坏反馈；闭环残余 3.8% 不阻塞 | CARD-001 回报数据；左编码器体检列入 CARD-002 步骤 0 | 继续追查传感器/重调电位器 |
| 2026-09-25 | 葫芦路线：查明队友版已实现 GW 门控+强转且实测失败过（0924 日志：IG 乱跳抹 GW + GZ 削顶），两根因恰被新基线修掉但未实车验证 → CARD-002 触发全关仪表跑拿干净数据，再定开判据④（GW）还是③（里程，24847 已作废待重量） | 失败数据在 zip 文档里（实车日志证据-葫芦圈-20260924.md），不浪费实车试错 | 盲目重开 GW 触发；沿用 24847mm 里程阈值 |
| 2026-09-25 | 记账待查（不阻塞葫芦）：0924 日志证据 A"全灭仍直行、丢线兜底没生效" | 车直着开出赛道=白丢一轮，但先保葫芦主线 | —— |
| 2026-09-25 | IG 清零改三条路径（CARD-003 Part A）：①GW 归零(数满)+连续普通线 100 拍=正常出圈（用户定义；直角弯图案非普通线→计数天然清零，"弯后直行100拍"自动成立，不用识别直角弯）②GW 未坐实保留旧快清（普通线20拍/TTL600）③IG=1 持续 3000 拍兜底强清 | 旧"普通线20拍"清是圈内 IG 乱跳 0 根因；但只留①③会让圈外误点的 IG 黏 30 秒→NO_CROSS 压死后续十字路口 | 字面"GW>6"：200mm 门槛下一切点计 1 次、GOURD_TOTAL=3，"数满"=归零事件；验证跑若实测每切点 2 次（6 行 GOURD-TANGENT）再改 GOURD_TOTAL=6，判据形式不变 |
| 2026-09-25 | 重启翻转修正过切点：GOURD_USE_FLIP=1 + GOURD_FLIP_ON_NORMAL=1，原参数（15拍×幅度3）先测，WIDE_ONE_GO_STRAIGHT 保持 0 | 车锁同一圆的病根=下一圆曲率反向、PD 只顺原圆绕；代码注释写明重试前提"先把 IG 做可靠"，恰被本卡 Part A+新基线（WHO/量程修复）满足；变量隔离，无效下卡再试直行窗口 | 0924 晚"翻转接上头晃更厉害"的旧账（根因是 IG 误点火，不是翻转方向错）；直行窗口单独用=换圆方向靠运气（run1 绕两圈才蒙过去） |
| 2026-09-25 | 直角弯=出状态机标志归【问题二】；本卡只做粗略清零定义，出口/减速细节下卡再写 | 用户指定拆分：先解决"平稳正确过完葫芦弯" | —— |
| 2026-09-25 | GW 双打判读：首过 3 切点干净单打三连（OD 1731/2425/3309，间距 694/884mm）GW 0→1→2→归零精确对齐过完葫芦；~220mm 双打仅绕圈重过切点时出现（翻转甩出又扫回）→ **GOURD_TOTAL=3 维持，"改 6"分支作废** | run4 23 行 TANGENT 分类 | 用户"每切点 2 次"的观察（实为绕圈场景）；CARD-003 C3 预判的改 6 路径 |
| 2026-09-25 | 出口位置定标：在最后圆上、**切点前 ~60mm**（run2 回放出口段 OD 19900-19917 vs 切点段 19955-19964）→ 待办第 2 条"GW 归零即强转"作废（归零时车刚进最后圆，出口在大半圈外），出口须图案+状态判 | run2 100Hz 回放 | 归零瞬间右转（会在最后圆内乱转） |
| 2026-09-25 | 0924 出口图案（左 2 路）误报病根：切点处左圆残线=左 2 路（run4 67.7s 实证），图案不可分；**可分特征 = 切点必翻号+必全灭，出口两者皆无**（run2 出口段 GZ 全正无翻号无全灭） | run2/run4 数据 | 沿用"左 N 路亮"图案判据（GOURD_EXIT_USE_PATTERN） |
| 2026-09-25 | 出口判据定稿（CARD-004）：宽贴一端+候选期满未翻号+期内未全灭+ge_done（GW 归零过）→ 右转 85°；ge_done 后减速至 65%；Path① 改挂"出口转完+直行 100 拍"（直角弯不用识别，弯道图案非普通线天然清计数） | 实测 Path① 现行版在圈内提前清 6 次；候选机制（cand_win/cand_ok）现成，翻号与否天然区分切点/出口 | Path① 维持"GW 归零+100 拍"；出口挂 GW 归零即转 |
| 2026-09-25 | 用户纠正 CARD-003 判读：切点"识别"对了但"行驶"未受控（15 拍翻转脉冲+PD 蒙过）→ 顺序过圆=运气；CARD-004 重构为切点行驶状态机（用户方案） | 用户现场目击；受控行驶才可复现，换电压/发车位不保证复现 | 军师把"顺序过圆"归因给翻转修正有效的判读 |
| 2026-09-25 | 切点状态机定稿：GW 奇=进切点(TANGENT)、偶=出切点(宽图案结束连续2拍确认)+按序号交替转向(1左/2右/3左，出切点起 15 拍×幅度3)；GOURD_TOTAL 3→6；GW==6→ge_done 减速迎出口；pending 配对锁存+30 拍超时 | run4 首过单打三连证明"出"不能靠二次 TANGENT；绕圈双打(220mm×5对)证明无锁存会数爆；方向按用户目击定标成单参数（实测反了翻一处符号） | 方向靠"进切点前误差反号"自动定（CARD-003 旧翻转，不可控）；方向按半圆自动判（无实测支撑） |
| 2026-09-25 | "从左/右半圆进入"作第二判据：先在 TANGENT 行加 side 遥测字段验证"1左3右5左"假设，不挡计数 | 数据驱动，先验证再上校验逻辑 | 立即用半圆校验纠错 |
