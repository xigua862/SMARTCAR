# 任务卡-CARD-001-回报
- 日期：2026-09-25　执行人：工人
- 结论：**完成**。Part A 队友固件已成 main 新基线并烧录冒烟通过；Part B T1/T2/T3 三组原始数据齐。两处偏差：① `WHO=70` 非卡预期的 68（IMU=1、功能正常，见下）；② T2 首轮数据被"左编码器线接触不良"污染，换线后已重测，作废数据也附在原样备查。

---

## 改动

| 项 | 内容 |
|---|---|
| A1 | `balance.c/h` 从仓库根目录移入 `参考资料/队友-平衡车IMU660RB参考/`；`.gitignore` 追加 `*.zip`；commit `0158c36`（基线快照） |
| A4 | 6 个差异文件全部用队友版覆盖：`app_config.h`、`telemetry.c`、`line_follow.c`、`line_follow.h`、`imu.c`、`car_fsm.c`；无新增文件；仓库无 zip 缺失文件 |
| FW_TAG | `0925-1738`（真实时间；队友版原为 0925-1545，已替换） |
| 编译 | `0 Error 0 Warning`，Code=25688 |
| commit | `08d2552`（已 push 到 origin/main） |
| 烧录 | ⚠️ 方式已变：**CMSIS-DAP**（队友的 DAPLink，Keil 里 Horco v0.2），命令 `openocd -f interface/cmsis-dap.cfg -c "transport select swd" -f target/stm32f1x.cfg -c "adapter speed 1000" -c "program ... verify reset exit"` → **Verified OK**。旧 ST-Link 命令已不适用 |

### A3 资产核对（逐条 grep 过）
- ✅ 相切点判据三条（`line_follow.c` 搜"相切点"15 处，宽+只贴一端/GZ±8拍翻号/事件后6拍无全灭都在）
- ✅ `LINE_OS_ENABLE/LINE_OS_SAMPLES/LINE_OS_SPACING_US/LINE_OS_VOTE_MAJORITY` + `LINE_SAMPLE_1KHZ`（`app_config.h`）
- ✅ `Q` 回放 + `TEST <秒>`（`telemetry.c`）
- ✅ 急停自动回放（`car_fsm.c` CAR_EMERGENCY 分支调 `telemetry_trace_dump()`）
- ✅ 别动清单逐值一致：`LINE_ACTIVE_LEVEL=GPIO_PIN_SET`、`LINE_CHANNEL_TABLE`=-7 -5 -3 -1 +1 +3 +5 +7、`ENCODER_PPR=422`、`MOTOR_LEFT_INVERT=1`、`MOTOR_RIGHT_INVERT=1`（两版逐字相同）

---

## Part A 冒烟（车静止上电，DAPLink 虚拟串口 COM11 @115200）

```
FW:0925-1738 ST=0 IR:00011000 RPM1=0 RPM2=0 OD=0 GZ=0 GW=0 IG=0 K1=0   （×N，全稳定）
RAW=00011000 ERR=0 BTN=0 ENC1=0 ENC2=3 WHO=70 IMU=1 GZ=0
```

- ✅ `FW:` = `0925-1738`（烧进去的确实是新固件；抓到的首行还残留旧固件 `0925-1545` 的尾巴，复位后即消失）
- ⚠️ `WHO=70 IMU=1`——卡预期 `WHO=68`。**0x70 是 MPU6500 的 WHO_AM_I**（0x68 是 MPU6050），模块应是 6500/兼容片；队友版"WHO 重试+兜底"判 IMU=1。功能证据：静止 `GZ=0`（零偏标定生效）。**请军师确认是否接受 0x70 为"正常"**
- ✅ 静止 `|GZ|=0 ≤ 3`
- ★附带发现：**DAPLink 虚拟串口的 PC→车 RX 是通的**（`H/M/T/O/Z/XX/TEST` 全部生效；此前 CH340 上 RX 不通的老病没有复现）。决策日志里"PC→车 RX 不通"的结论在 DAPLink 链路上不成立。仅出现过 1 次 `Z` 被噪声吞掉（回了 CMD 帮助行），重发即好

---

## Part B 原始数据

### T1 电气中心（车静止不启动，尺量阵列中点对线，7 位置 × 2 次；单位：位移 mm / RAW 位图 / ERR）

| 位移 | 第1次 | 第2次 |
|---|---|---|
| 0（中点） | RAW=00011000 ERR=0 | RAW=00011000 ERR=0 |
| 左 5 | RAW=00011000 ERR=0 | RAW=00011000 ERR=0 |
| 左 10 | RAW=00001100 ERR=4 | RAW=00001100 ERR=4 |
| 回中点 | （复测同 0mm：00011000 / 0） | |
| 右 5 | RAW=00111000 ERR=-3 | RAW=00111000 ERR=-3 |
| 右 10 | RAW=00110000 ERR=-4 | RAW=00110000 ERR=-4 |

- 中心处 `ERR=0`，`RAW=00011000`（中间两路）✅ 落在卡的期望值上
- 目测：阵列 PCB 未见肉眼可见的装歪/偏离底盘中线（摆放时用户确认）
- 灵敏度参考（原始推算，未加工进结论）：左右各 10mm → ERR ±3~4

### T2a 开环（等 PWM）

**架空**（单位：PWM / RPM）：

| PWM | RPM1 | RPM2 |
|---|---|---|
| 20 20 | 99~113 | 71~85 |
| 25 25 | 127~142 | 113~127 |

- 同 PWM 空载转速差：左比右快约 10~15%（RPM2/RPM1 ≈ 0.72~0.89，随档变化）
- 定档 PWM=25（最接近 150 RPM）

**落地直行**（`Z` 清零后 `M 25 25`；⚠️ 本次 Z 被串口噪声吞掉，但因重新上电里程本就从 0 起，起步时 OD=58 为摆放挪动）：
- `ODOM NOW: OD=5941mm PATH=5941mm L=-5343 R=-5875`（mm）
- 目视（用户原话）：**"直线稍微左偏"**（起步撞墙一次，重新放到大平地后测得）
- 左右差：|R|/|L| = 5875/5343 ≈ **+10%**（R 多）

### T2b 闭环（等目标速，`T 1 30` + `T 2 30`，8 秒定时自动 XX）

- 过程短行：`RPM1=85~113 RPM2=85~99 GZ≈0`（起步瞬间 GZ=-54，之后 0/±7，无单向持续漂）
- `ODOM NOW: OD=2940mm PATH=2940mm L=-2864 R=-2974`（mm）
- 左右差：|R|/|L| = 2974/2864 ≈ **+3.8%**（>2%，触发卡的 H1 判据线，供军师判读）

### ⚠️ T2 作废数据（换线前，左编码器接触不良期间，原样存档）
- 故障：T2b 中 `RPM1=0~14`、`L=0`，`R=-12386`、`OD=6595`；用户拉扯线时**电源灯忽暗忽亮** → 换线后牢固，故障消失
- T2a 首跑：`OD=3587 L=-1581 R=-5087`，目视"车在右转"——L 偏小正是左编码器丢计数的样子，与换线后数据（L≈R）互相印证
- 备注：换线前"L/R 与物理转向相反"的矛盾，换线后不再出现
- 里程 `L/R` 符号为负是 `ODOM_SIGN` 与编码器接线方向相反（`odom.c` 注释已自述），OD 走绝对值不受影响；判读看绝对值

### T3 闭环平衡点存档（`TEST 5` 循迹直道 5 秒自动停，中途 2.5s 发 `H`）

- 里程：OD 2959 → 5711（跑了 2752mm）
- 中途 H：`IR:00011100 ERR=3 RPM1=14 RPM2=14 GZ=0`（⚠️ 抓拍时刻车未在全速段，RPM 不代表稳态；稳态看轨迹）
- 100Hz 轨迹 501 行存 `_tmp/t3_trace.txt`；稳态段（T100~T107 抽样）：
```
T100 IR:00011100 GZ=  -31 OD=3445
T102 IR:00011000 GZ=  -31 OD=3452
T104 IR:00011000 GZ=  -7  OD=3461
T105 IR:00011000 GZ=   0  OD=3468
T107 IR:00011000 GZ=  31  OD=3475
```
- 稳态 `IR` 在 `00011000`/`00011100` 间摆动（中间两路为主，偶尔右多一路），**不再是卡背景里说的 L2/L3（ERR≈-8）偏左**；`ERR=+3`；`GZ` 在 ±31 内小幅摆动

---

## 验收逐条对照

**Part A**
- ✅ 冒烟 `FW:` = 新 FW_TAG（0925-1738）
- ⚠️ `WHO=68 IMU=1` → 实际 `WHO=70 IMU=1`（MPU6500；功能指标 GZ=0 达标，待军师拍板）
- ✅ 静止 |GZ| ≤ 3（=0）
- ✅ 编译 0E0W；commit 写明取自队友版的文件；已 push

**Part B（数据质量）**
- ✅ T1 14 行（7 位置 × 2）
- ✅ T2a：PWM=25、空载 RPM1/RPM2（两档）、落地 L/R、目视方向
- ✅ T2b：目标速 30、过程 RPM/GZ、落地 L/R
- ✅ T3：稳态一组（H + 轨迹文件）
- ✅ 单位已标（mm / RPM / 位图）；原始值未加工

## 卡点
1. `WHO=70` 与卡预期 `68` 不符——功能正常，需军师确认判读口径。
2. T3 的 `H` 抓拍时 RPM=14（非全速稳态），如需"全速稳态 RPM/ERR"快照，可用 DAPLink RX 通的条件在 RUN 中补发 `H`（下张卡一句话即可）。
3. 烧录链路和串口链路都已换成 DAPLink（一条 USB 同时干烧录+串口，地环路问题天然消失），旧文档第七节的 ST-Link/CH340 流程已过时，建议军师更新文档。
