# 烧录方式变更：ST-Link → CMSIS-DAP（DAPLink）

- **生效日期**: 2026-09-25
- **原因**: 原来的 ST-Link（SN 尾号 `ECCE1A43`）换掉了，改成 CMSIS-DAP / DAPLink

---

## 1. 变更前后对比（很重要，旧笔记已失效）

| | 旧（ST-Link） | 新（CMSIS-DAP / DAPLink） |
|---|---|---|
| 硬件 | ST-Link V2，SN `37FF71064E573436ECCE1A43` | CMSIS-DAP 下载器，SN `1582521132` |
| 烧录命令 | `ST-LINK_CLI.exe -c SWD UR -P x.hex -V -Rst` | `UV4.exe -f x.uvprojx` |
| 回读命令 | `ST-LINK_CLI.exe -r8 0x08002000 0x3FFF` | **没有了** ✗ |
| 逐字节校验 | `_work/verify_build.js` 比对设备 vs hex | **做不了** ✗ |

## 2. 现在的工作流

```powershell
# 编译
& "C:\keil5\UV4\UV4.exe" -r "<repo>\MDK-ARM\test16.uvprojx" -j0 -o "$env:TEMP\x.log"
# 期望: 0 Error(s), 0 Warning(s)，exit=0

# 烧录（会先编译再下载）
& "C:\keil5\UV4\UV4.exe" -f "<repo>\MDK-ARM\test16.uvprojx" -j0 -o "$env:TEMP\flash.log"
# 期望日志: Load "test16\test16.axf" / Erase Done. / Programming Done. / Verify OK.
```

## 3. 版本验证方式（因为回读没了）

1. **Keil 的 `Verify OK`** —— 下载后自动比对 FLASH 与 axf ✓
2. **固件自报的 `FW:` 标签** —— 串口第一行，最硬 ✓
   （改代码时务必同步更新 `app_config.h` 的 `FW_TAG` ✓）

## 4. Keil 里的设置（已保存，别再动）

存在 `MDK-ARM\test16.uvoptx`：

```xml
<pMon>BIN\CMSIS_AGDI.dll</pMon>          ← 调试器 = CMSIS-DAP
<TargetDriverDllRegistry>
  <SetRegEntry><Key>UL2CM3</Key>         ← 旧的 ULINK 配置（留着无害）
  <SetRegEntry><Key>ST-LINKIII-KEIL_SWO</Key>  ← 旧的 ST-Link 配置（留着无害）
  <SetRegEntry><Key>CMSIS_AGDI</Key>     ← ★新增，Keil 自动生成
```

## 5. ⚠️ 一个必须记住的坑

商家给的配置图是 **STM32F407VGT6** 的，本工程是 **STM32F103C8**：

- **Debug 页**的设置跟芯片无关，可以照抄 ✓
- **`Flash Download` 页的下载算法千万别照抄** ✗
  本工程必须是 **`STM32F10x_128`**（`STM32F10x_128.FLM`）✓

## 6. 踩过的弯路（省得再走）

- 改 `.uvprojx` 的 `<TargetDllName>` → 报 `BIN\BIN\... not found` 或 `invalid debug Dll` ✗
  （那里只是 AGDI 内核，**调试器的选择在 `.uvoptx` 的 `<pMon>`**）
- 只改 `<pMon>` 不补 `<TargetDriverDllRegistry>` → 报 `Internal DLL Error` ✗
- 手改 `.uvoptx` 半天，不如 **GUI 里点一次 OK** —— Keil 会自动把 `<pMon>` 和 `<Key>` 都写对 ✓✓✓

## 7. 沙箱限制（环境相关）

- 工作区路径含中文 → spawn 的 python 收到乱码路径，venv 建不起来 ✗
  （所以没走 pyOCD 路线；真要用，得把环境建在纯英文路径下）
