# 提速情报 · 从三个竞赛开源包里扒出来的（2026-09-24）

> 来源：`Desktop\test17\参考资料\` 下三个开源包，逐个文件读过（团队代码在各自工程的 `code\` / `user\cyh_code\`）：
> ①`马铃薯-疯狂电路国赛光电管软件开源-5.5m\` ②`马铃薯-疯狂电路区赛摄像头软件开源-4.5m\` ③`纯跟踪算法-19届负压镜头源码\` + `CYH-NYY19届负压镜头纯跟踪算法\`
> **本文只做情报整理，未改动任何代码。**

---

## 〇、先纠正三个数字（别被包名骗）

| 包名写的 | 固件里实际 | 证据 |
|---|---|---|
| 光电管 **5.5 m/s** | 随固件发布的地图**最高速 420 cm/s = 4.2 m/s**；5.5 只在地图生成器配置（`地图生成器\juesai.json` 的 `max_speed_cmps=550`）| `code\nav_path.h:35` |
| 摄像头 **4.5 m/s** | 固件里**基础速度 = 350 cm/s = 3.5 m/s**；4.5 只出现在注释里（"提速也先保持 150"）| `control.c:193`、`navigation.h:97-99` |
| 19届"负压镜头**纯跟踪**" | 它**不是经典 Pure Pursuit**：取中线上一个前瞻点 → `atan2` 求偏角 → PD 输出舵机角；**不算曲率**。而且**差速被乘 0 关掉了**，纯靠舵机转向 | `user\cyh_code\cyh_camera.c:659-743`、`cyh_motor.c:117-123` |

---

## 一、★核心结论：他们为什么能快（架构层面，不是技巧）

**三台车全部"不靠传感器循迹"：**

| 车 | 转向量从哪来 |
|---|---|
| 国赛光电管 4.2m/s | **离线地图曲率前馈 + 陀螺横摆率闭环 + 里程计路径投影**。光电只用来定位/保护，"不直接生成控制目标"（`navigation.c:26-29`）|
| 区赛摄像头 3.5m/s | **离线路径回放 + 编码器航迹推算 + Pure Pursuit（150mm 前瞻）**。摄像头降级成"低速横向位置修正器"，**只在直道区段才被采纳**（9 道门：`navigation.c:478-550`）|
| 19届 250 速 | **前瞻点 → 偏角 → PD → 舵机**（固定前瞻 18 点）|

→ **"读不到线"在三台车上都不是失效模式**：线传感器不在控制环里。这是他们敢上速度的**根本原因**，也是你当前症状（1.2m/s 读不到线）与他们的差别所在。

**支撑他们的四个共同项：**
1. **1kHz 感知 + 1ms 控制**：光电扫描 1kHz（`phototube_array.h:39`）、控制/导航 1ms（`isr.c:63-68`、`control.h:36-37`）。4.2m/s 下**每拍只走 4.2mm < 阵列间距 8mm**。
2. **离线速度表**：逐点速度烘进 `nav_path.h`（96 段，420cm/s 上限，横向 11760 cm/s²，a+=3000 / a-=5000 cm/s²）；车端只做 1ms 的时间域限幅（`control.c:275-361`）。
3. **实测标定的延迟补偿**：执行器提前 **22ms** 下发（`competition_config.h:78`）、曲率提前 **v×35ms**（4.2m/s ≈147mm，`competition_config.h:45-53`）。
4. **负压吸附（物理前提）**：国赛运行档 80%、预热 2s、稳定 500ms 才发车、**停车后保持 500ms 防点头、堵转立即关负压**（`prestart.c:594-631`、`competition_config.h:254-258`）；区赛 100% 吸附（`prestart.c:53`）。

---

## 二、★能直接搬到你 F103 + 8 路数字红外 + 100Hz 车上的招（按性价比）

### P1 ★★★ 把"读线 + 转向"挪进 1kHz 节拍（不是算法，是频率）
- 8 路 GPIO 直读只需几 µs；作者是光电 1kHz + 控制 1ms。
- **算给你听**：你 1.2 m/s、10ms 一拍走 **12mm** ≈ 半个线宽（25mm）→ 这就是"来不及"的算术原因。1kHz 后每拍只走 **1.2mm**。
- 依据：`phototube_array.h:39`、`isr.c:63-68`。
- 代价：低（TB6612 的 PWM 可以 1kHz 更新；注意 F103 无 FPU，1ms 环里别放大浮点运算）。

### P2 ★★★ 丢线/元素判定"按距离"而不是按帧数（速度无关）
- 作者全黑停车按 **200mm 里程**判定（不是 N 帧）：`competition_config.h:57`、`task.c:744`。
- 短暂跳变**保持上一帧 60ms**：`competition_config.h:119`（你 1kHz 化后 = 60 拍）。
- 预期：十字/Ω 入口不再"瞬间丢线急停/乱打"。

### P3 ★★★ 前瞻别固定死——做"速度 → 用第几路探头"的小表
- CYH 的前瞻是**固定下标 18**（`cyh_anniu.c:59`），**代码里没有任何随速度自动调整前瞻的逻辑**（这是他们没做成的事）。
- 你的红外天然等距 → **一张小表就能实现"速度变→前瞻变"**，比开源做得更好。
- 三个开源车都是"前瞻距离随速度放大"：国赛 `v×35ms`、区赛 `NAV_PP_LOOKAHEAD_MM 150`、CYH `18×4=72` 逆透视像素。

### P4 ★★ 速度环：1kHz + 增量式 PI + 单拍增量限幅 + 堵转保护
- 增量式 PI（天然抗积分饱和）：`PID.c:47-64`（`IncrementLimit` 单拍限幅）。
- 堵转保护两版可抄：
  - 国赛：**高 PWM + 目标 ≥120cm/s + 实测 < 目标 40%（或反向）持续 60ms** → 停车（`competition_config.h:59-67`）
  - 区赛：**占空比 ≥ 软限幅 70% 且编码器 ≈0 持续 100ms** → 停车（`control.c:50-54,341-382`）
- **速度环参数按电压分档的一手记录**（注释里）：`7.2v：30,25,20` / `12v：190,30,10`（`cyh_motor.c:17-18`）→ **换电压必须重调速度环，而且高压档 KP 反而要大幅加大**（你正考虑 12V 电池，这条直接相关）。

### P5 ★★ 转向：三个可直接用的式子
1. **非线性大误差增强**：`u = KP·e + KP2·e|e| + KD·Δe + GKD·gyro_z`（`PID.c:84`）——★**你有 IMU（MPU6050）**，`GKD·gyro_z` 这一项正好能加上抗过冲。
2. **转向量限幅 = 当前基础速度**：保证左右轮目标不为负（`control.c:303-321`，`TURN_CONTROL_LIMIT_RATIO 1.0`）——防止低速抽头/高速打不动。
3. **线性外推当前瞻**：`err_used = e + k·(e - e_prev)`（就是 D 项当提前量）。⚠️ 你 8 路二值量化台阶大，D 项容易"抽"→ 先只做"增益除以速度"，D 项压小或关掉（CYH 的 D 从 22.6 调到 15，就是被抖出来的）。

### P6 ★★ 元素处理的两个通用写法
1. **元素互斥优先级**：所有元素入口都带 `&& 其它状态==0`（`cyh_yuansu.c:159,214`）→ **同一时刻只允许一个元素态**，直接消掉一大类误判。
2. **元素状态机用编码器脉冲累加当里程**：`xx += speedl+speedr` 判"进入/保持/退出"（`cyh_yuansu.c:43,93,144,178,224`），阈值按你的轮径×422 线换算即可。**你已有编码器，移植成本几乎为零。**
3. **过元素后强制低速一段固定时间**：CYH 斑马线后 `jianms>=500` 拍才允许再加速（`cyh_yuansu.c:63-74`）——你 100Hz 等价 **250 拍**。**这是"提速后不失控"的关键保护。**

### P7 ★ 绕障"改前瞻点坐标"一行搞定
`Rout_MID[yumao][0] += 20`（`cyh_yuansu.c:410-414`）——不是重规划路径，直接把前瞻点横移。你的等价操作 = 绕障时把加权中心整体偏移 N 个探头。

### P8 ★ 负压时序（★这条是给你"负压吸附新车"的）
一手记录（`国赛代码流程与算法总结.md` 原文）：
> "停车时**先停轮、保留负压，500ms 后再关负压**，避免吸附突然消失而点头"
> "堵转时**立即关负压**，避免吸附把已经卡住的轮子压死"

参数：预热 2000ms → 运行档 80% → 稳定 500ms → 发车（`prestart.c:606`、`competition_config.h:254-258`）；区赛 100% 吸附。

### P9 ★ 两条工程纪律（作者特意写进代码/文档的）
1. **"不要只看 `flash.c` 默认值就判断实车参数"**——转向 KP/KD/KP2/GKD 源码默认全 0，真实值在车上 Flash 里（区赛总结原文）。
2. **"变量名不代表功能还在跑，要沿当前调用链看"**——国赛包里有 `VisionPoseObservation`、`NAV_PP_*` 这种名字，但摄像/纯跟踪根本没参与控制（国赛总结原文）。**这条对你那 1332 行的 屎山同样适用：判断一个模块有没有用，看调用链，不看名字。**

---

## 三、搬不了的（硬件档次差，别硬凑）

1. **离线地图 + 定位闭环整套**：国赛 1280 点在线点云 + 鲁棒 SE(2) 匹配 + 60 段接管区 + HiGHS 时间最优求解器；`nav_path.h` 一张表 9376 点，**远超你 20KB RAM / 64KB Flash**（单条约 30KB）。要搬必须降到 10–20mm 采样或用 int8 差分编码。
2. **双核 + FPU + 200MHz**：TC264 两核分工（CPU1 专跑图像/1kHz 扫描，CPU0 跑 1ms 控制）。F103 单核 72MHz 无 FPU，`atan2f/cosf` 不能进 1ms 环。
3. **MT9V03X 150×80 图像链路**：单幅 12KB + 二值图/边界/中线，**20KB RAM 放不下**。整节图像/标定/八邻域/逆透视对 8 路二值红外**零可移植性**。
4. **陀螺 VQF 480Hz**：横摆率闭环、滑移阻尼都建立在它上面（好消息：区赛那版**航向也不用陀螺**，用编码器差速，`GYRO_YAW_SOURCE_ENCODER_ONLY=1`）。
5. **负压 + FOC 电调 + 17kHz PWM + 1024 线编码器**：4.2m/s 过 194mm 紧弯的物理前提。你 7.4V + TB6612 + 422 线的天花板由机械决定，**代码只能逼近不能突破**。

---

## 四、★最值钱的两个文件（一手总结，优先读这两份）

1. `马铃薯-疯狂电路国赛光电管软件开源-5.5m\国赛光电管\国赛代码流程与算法总结.md`
2. `马铃薯-疯狂电路区赛摄像头软件开源-4.5m\区赛摄像头\区赛代码流程与算法总结.md`

**它们里面有"提速该怎么改"的原话**，例如（照抄）：
> "曲率提前量…**当前 35ms 是针对 4.2m/s 基线调的，约 147mm。后面如果提速到 5m/s，应先把时间降到 29~30ms，一次只动 2~3ms。**"
> "**车速超过 300cm/s、横摆率超过 600°/s**，且编码器推算与陀螺相差 >250°/s 时，按真实横摆方向加反向阻尼，**最大附加半轮差 25cm/s**，差值回到 180°/s 以下才解除。"
> "（区赛）用 **320ms** 把基础速度从 0 平滑升到 **350cm/s**"、"像素偏差按实测 **2.17mm/px** 换算，修正限制在 **500mm/s 注入速度**内"。

⚠️ 另外：你期待的"按速度档整理的调参笔记"**不存在**——`CYH-NYY…\` 里的 txt 全是**代码摘录**，`120/150/140` 那几个数字文件夹是**镜头视场角**（120°/150°/自制140°）的逆透视样本图，不是速度档。**真正的调参痕迹只有注释里的旧值**（如 `P_fx=1.85 //1.56`、`D_fx=15 //22.6`、`jiasu=0 //5`）。

---

## 五、给你的下一步（按"你现在的车"排）

**先说结论：你现在 1.2m/s 读不到线，主因很可能是"采样/控制周期太慢"（10ms 走 12mm ≈ 半个线宽），不是算法不够聪明。**

| 顺序 | 做什么 | 为什么先做这个 |
|---|---|---|
| 1 | **读线+转向进 1kHz**（P1），并把**丢线/元素改按里程判**（P2） | 直接打在"来不及"的病根上，不依赖任何新硬件 |
| 2 | **过元素/噪声后强制低速一段**（P6-3） | 让你敢提速的前提是"出错能兜住" |
| 3 | **前瞻随速度换探头**（P3）+ **转向量限幅=基础速度**（P5-2） | 两条小改动，直接换速度 |
| 4 | **速度环 1kHz + 增量限幅 + 堵转保护**（P4） | 把电机"榨干"的部分 |
| 5 | 12V 电池那步之前，先按 **P4 的电压分档结论**重调速度环 | 否则换电压必失控 |

**至于"江郎才尽"**：这三台车摆在这，说明**不是想法不够，是没拿到别人的参数**。你要的"黄金"其实就两类——**别人的频率（1kHz）、别人的常数（24847mm、2.17mm/px、22/35ms、3000/5000cm/s²）**。以后每拿到一个开源包，先去找这两类东西，比读算法更有用。

---

*本文只做情报整理，未改动任何代码。*
