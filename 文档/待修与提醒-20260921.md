# 待修与提醒 — 2026-09-21

> 来源：ZCode 代码复查（读了 `Core/App/control/line_follow.c`、`Core/App/fsm/car_fsm.c`、
> `Core/App/app_config.h`、`Core/App/telemetry.c` 之后整理的）。
> **约定：这个文件只记录问题 + 复现/验证方法，不改代码。** 改不改、怎么改自己定。

---

## 🔄 2026-09-22 复查更新（通读整个工程后）

**老条目现状**

| 原条目 | 现状 |
|---|---|
| 一、丢线 uint8 回绕 | ✅ **已修**：`lost_cycles` 改成 `uint16_t`，超限时钉死在 `LOST_MAX_CYCLES+1` → "停车防跑飞"现在真的生效。（原复现步骤留作回归测试用）|
| 二、葫芦靠哪一层过（纯兜底） | 🔄 **已被新方案取代**：9/22 固件有了**"葫芦弯相切点状态机"**（`USE_GOURD_SM`）|
| 三、搬 test161 的固定数组 | ⏭️ **没搬，也不建议搬** —— 现在这套"相切点计数 + 翻转修正"更贴合你的车（不绑速度）|

**新增（按重要性排序）**

### A. ⚠️ 葫芦状态机可能"误触发"，且你现在的遥测就能验证
判据是"**最外路亮(≤2 路) → 3 拍内扩散到 ≥3 路**"。但**直角弯入口、十字入口、丢线恢复**都会产生同样的"边缘→扩散"波 → 都会被记成一个"相切点"，并在之后 15 拍内一旦丢线就**反向修正**（`-gourd_dir*3`）。赛道上有 **3 个直角弯 + 2 个十字**，撞上的机会不小。

**验证（免费，不用改代码）**：跑完整圈，看遥测里的 **`GW`** 值。
- 正常应该是 **3**（4 个相切圆 = 3 个相切点），绕完清零；
- 如果在直角/十字处 `GW` 乱涨（比如跑到 5、6 然后清零），就是误触发已经在发生。

**若要加闸门**：用你刚启用的 **IMU** 最省事 —— 真相切点处**偏航率 `GZ` 会翻转符号**（左转→右转），直角弯处不会翻。`GZ` 已经在遥测里印了，直接可用。只在"GZ 符号翻转"时允许开 `gourd_flip` 窗口，误触发就能压掉一大半。

### B. 🔧 魔数 `3` 没进 `app_config.h`
`line_follow.c:136` 和 `:182`：`e = (int8_t)(-gourd_dir * 3);` —— 这个"翻转修正力度"是**硬编码**的，改它必须动 `.c`。这违反本项目"**唯一调参入口 = app_config.h**"的约定（你自己在 README 里写的）。建议提成 `GOURD_FLIP_ERR`。

### C. 🔧 直线提速是"瞬跳"，没有斜坡
`sp = STRAIGHT_BOOST_SPEED(99)` 一满足条件直接从 70 跳到 **99**（满 PWM），退出却是"误差一大立刻回落" = 慢起快落。
→ 如果提速那一瞬间车会**一顿/摆一下**，先别怀疑 KP，给个斜坡（每拍 +2~3）试；另外 99 已是满 PWM，留点余量（95）通常更稳。

### D. 📄 注释与实现不符（会误导下一个人）
`line_follow.c:68` 注释写"3 次 = 绕完 4 个圆 → **退出**"，但代码只是把 `gourd_waves` 清零——**没有"退出"状态**（这个 SM 是无状态的建议器，只影响丢线时的修正方向）。行为没问题，注释该改。

### E. ✅ 位序核对通过（读日志时按这个读）
`line_sensor.c:23` 用 `r.raw |= 1u << i`，`i` 跟着 `LINE_CHANNEL_TABLE`（左→右）→ **bit0 = 最左（权重 -7）、bit7 = 最右（+7）**。
所以遥测里 `IR:` 的**第一个字符就是最左侧那一路**，`IR:10110000` = 从左数第 1、3、4 路亮。`raw & 0x01 / 0x80` 用得对，没问题。

### F. ⚠️ 串口 RX 的状态自相矛盾，要确认
`README.md`（9/22）把串口命令表当可用功能写；`代码交接说明-20260921.md` 的"已知问题"里写着 **"串口 RX 收不到 → 待修"**。
**如果 RX 真收不到，`P/S/D` 在线调参就全废**（只能改宏重烧），调车效率差一个量级。→ 插上串口发一条 `V`，看有没有回；不会回就是还没修好。

### G. ⚠️ 检录硬要求：板子上的"丝印 logo"
`代码方案-20260920.md` §一 抄的规则原文要求：电路板（主控/驱动/传感器任一）必须有 **"2026 机器车循迹 + 队伍名"丝印 logo**；§五 Q6 问过这件事，但**后续文档里没看到结论**。
这不属于代码问题，但**现场会直接卡检录**。→ 打开 `硬件设计/寻迹小车板子V3_PCB1_*.zip` 的丝印层看一眼，或者直接看板子实物。没有的话只有两条路：贴标 / 下一版加印（= 又一趟打样 + 快递）。

---

## 一、待修 Bug：丢线"停车防跑飞"被 uint8 回绕抵消了

**位置**
- `Core/App/control/line_follow.c:15` —— `static uint8_t lost_cycles = 0;`
- `Core/App/control/line_follow.c:86-99` —— 丢线旋转 / 停车分支
- `Core/App/app_config.h:64` —— `#define LOST_MAX_CYCLES 150`

**当前代码**

```c
if (line_lost)
{
  if (lost_cycles > LOST_MAX_CYCLES) { motor_stop(); }        /* 设计意图: 找不到线就停车防跑飞 */
  else if (last_error >= 0) { motor_set_differential( 35, -35); }
  else                      { motor_set_differential(-35,  35); }
  return;
}
```

**问题**：命中停车分支后 **`lost_cycles` 没有重置**，而它是 `uint8_t`。

| 拍数 | 行为 |
|---|---|
| 0–2 | 短暂丢线：`e = last_error × 2` 沿原方向修正 |
| 3–150 | 原地旋转找线（±35） |
| 151–255 | `motor_stop()` 停住 |
| **256（回绕）** | `lost_cycles` 变成 0 → **又回到"旋转"** → 再停 → 再转… |

**实际行为**（1 拍 = `CTRL_PERIOD_MS` = 10 ms）：**转 1.5 s → 停 1.05 s → 又转 1.5 s → …** 无限循环，永不真正停住。
→ "防跑飞"这个安全设计**被类型回绕抵消**了：丢线时车会在原地一直打转。

**两种改法（选一个，并把意图写清楚）**
- **想真停**：`lost_cycles` 改 `uint16_t`，或在停车分支里把它钉死（`lost_cycles = LOST_MAX_CYCLES + 1;`）+ 给个 `stopped` 标志；
- **想要脉冲搜索**（"停一会再找"）：把现在这个行为**显式写成**"停 N 拍后重试"，别靠类型回绕来兜。

**2 分钟复现**：车架空（四轮离地）、用纸/手把 8 路传感器全挡住 → 计时看轮子：
按设计应约 1.5 s 后停住；**实际是停 ~1 s 后又自己转起来**，如此反复。

---

## 二、待确认：葫芦弯到底是靠哪一层过的（test17 里**没有**葫芦状态机）

`line_follow.c` 里只有四样东西，没有 GOURD 状态：速度自适应 / 十字判定 / 丢线兜底 / PD。
能过葫芦靠的是三套**通用**兜底凑出来的：

| # | 位置 | 干什么 |
|---|---|---|
| ① | `line_follow.c:38-51` | 速度自适应：`\|e\| ≥ CURVE_ABS_ERR_THRESH(3)` → 降到 `sp_curve`（60 → 42） |
| ② | `line_follow.c:68-72` | 短暂丢线：`e = last_error × 2`（沿上次方向**加倍**修正） |
| ③ | `line_follow.c:86-99` | 持续丢线：原地旋转找线 |

**验证方法（别推理，用数据）**：跑动时遥测是 **20 Hz**（`app_config.h:54`，`TELEMETRY_PERIOD_RUN_MS 50`）。
把过葫芦那几秒的串口日志存下来，看 `IR:` 位图：

- **从不出现 `00000000`** → 是 **①降速 + PD** 过的，兜底没参与（说明当前几何/25 mm 线宽下葫芦并不难）；
- **全 0 后 1–2 拍恢复** → **②在救**；
- **全 0 且长时间维持**（`ST` 停在丢线态很久）→ **③在救** → 同时去看第一节那个 Bug。

**第②层的失效模式**：`e = last_error × 2` —— 如果丢线那一刻车**正好居中**（`last_error ≈ 0`），
翻倍还是 0 → **不修正，直着冲出去**。相切点恰好压中心时就会撞上。

---

## 三、以后把 test161 的葫芦逻辑搬进来时，注意三件事

参考实现：`..\test161\Core\Src\main.c`（**参数在第 42–47 行，状态本体在 169–201 行**）
`GOURD_ENTRY_HOLD 3` / `GOURD_STEP_BEATS 4` / `GOURD_STEER 30` / `GOURD_SPEED_PCT 60` / `GOURD_MAX_CYCLES 150`

1. **图案撞车（最危险）**：test161 里 终点 = `10101`、葫芦 = `00101`，**只差最左一路**，而 TRACK 中**先判终点**。
   进葫芦时车位稍歪、最左被扫到 → 会被判成**终点 → 停车自锁**。
   → 解药就是你新 FSM 里留的那个 TODO：**终点 = 图案 + 里程双条件**（`Core/App/fsm/car_fsm.c:128`）。**先做这个，再搬葫芦。**
2. **别按"拍"打节奏**：test161 那个固定数组是按 10 ms 拍数计的 → **绑死速度**，一改 `BASE_SPEED` 就得重调 `GOURD_*`。
   → 改成按**编码器脉冲（距离）**计数：编码器已配好（`app_config.h:112-120`，TIM3/TIM4 正交解码，422 PPR）。
   正好是 W3 那条线，而且不再绑速度。
3. **保留现有兜底**：test17 这套"降速 + 丢线加倍修正 + 旋转找线"对入口角度更鲁棒。
   搬状态机时**把它留作 fallback**（认不出葫芦就照常循迹），不要只留开环节奏。

---

*本文件只记录问题与验证方法，不含任何改动。*
